name: Execute resuable workflow
on:
  workflow_dispatch
jobs:
  call_reusable_workflow:
    uses: ./.github/workflows/resuable-workflow.yml
    with:
      username: Vinit
    secrets:
      envPAT: envPATValue
